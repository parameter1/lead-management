{{#link-to
  "link.tracking.ad-creatives.edit" item.id
  class="list-group-item list-group-item-action flex-column align-items-start"
}}
  <div class="d-flex w-100 justify-content-between">
    <h4 class="mb-1">{{item.customer.name}}</h4>
    {{#if item.updatedAt}}
      <small>{{entypo-icon "clock"}} Updated {{moment-format item.updatedAt "MMM Do, YYYY h:mm a"}}</small>
    {{/if}}
  </div>
  <p class="mb-0">URL: {{item.url}}</p>
  <p class="mb-0">
    Tags:
    {{#each item.tags as |tag|}}
      {{tag.name}}
    {{else}}
      (none)
    {{/each}}
  </p>
  <p class="mb-1">Description: {{if item.description item.description "(none)"}}</p>

  <small>ID: {{item.id}}</small>
{{/link-to}}
