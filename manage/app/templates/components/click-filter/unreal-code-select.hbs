{{#power-select-multiple
  disabled=disabled
  options=unrealCodes.options
  selected=labels
  onchange=(action onChange)
  placeholder=placeholder
  closeOnSelect=false as |label|
}}
  {{ label }}
{{/power-select-multiple}}
