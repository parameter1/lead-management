<nav class="navbar navbar-expand-xl navbar-dark bg-dark">
  {{#link-to "index" class="navbar-brand"}}<span class="text-primary">Lead</span> Management{{/link-to}}
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      {{#if user.isAtLeastMember}}
        <li class="nav-item">
          {{#link-to "dashboard" class="nav-link"}}{{entypo-icon "blackboard"}} Dashboard{{/link-to}}
        </li>
      {{/if}}

      {{#if user.isAdmin}}
        <li class="nav-item">
          {{#link-to "customer.list" class="nav-link"}}{{entypo-icon "v-card"}} Customers{{/link-to}}
        </li>
      {{/if}}

      {{#if user.isAtLeastMember}}
        <li class="nav-item">
          {{#link-to "order" class="nav-link"}}{{entypo-icon "credit"}} Orders{{/link-to}}
        </li>
      {{/if}}

      <li class="nav-item">
        {{#link-to "campaign" class="nav-link"}}{{entypo-icon "calendar"}} Campaigns{{/link-to}}
      </li>

      {{#if user.isAdmin}}
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="javascript:void(0)" id="linksDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            {{entypo-icon "link"}} Links
          </a>
          <div class="dropdown-menu" aria-labelledby="linksDropdown">
            {{#link-to "link.tracking" class="dropdown-item d-flex justify-content-between"}}Track {{entypo-icon "code"}}{{/link-to}}
            {{#link-to "link.urls" class="dropdown-item d-flex justify-content-between"}}Manage {{entypo-icon "network"}}{{/link-to}}
            {{!-- {{#link-to "email.category" class="dropdown-item d-flex justify-content-between"}}Host {{entypo-icon "tag"}}{{/link-to}} --}}
          </div>
        </li>
      {{/if}}

      {{#if user.isAdmin}}
        <li class="nav-item">
          {{#link-to "identity" class="nav-link"}}{{entypo-icon "users"}} Leads{{/link-to}}
        </li>
      {{/if}}


      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="javascript:void(0)" id="navEmailDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          {{entypo-icon "email"}} Email
        </a>
        <div class="dropdown-menu" aria-labelledby="navEmailDropdown">
          {{#if user.isAdmin}}
            {{#link-to "email.send" class="dropdown-item d-flex justify-content-between"}}Sends {{entypo-icon "paper-plane"}}{{/link-to}}
            {{#link-to "email.deployment" class="dropdown-item d-flex justify-content-between"}}Deployments {{entypo-icon "mail"}}{{/link-to}}
            {{#link-to "email.category" class="dropdown-item d-flex justify-content-between"}}Categories {{entypo-icon "tag"}}{{/link-to}}
          {{/if}}
          {{#link-to "email.reporting" class="dropdown-item d-flex justify-content-between"}}Reporting {{entypo-icon "bar-graph"}}{{/link-to}}
          {{#link-to "email.click-events" class="dropdown-item d-flex justify-content-between"}}Click Events {{entypo-icon "mouse-pointer"}}{{/link-to}}
        </div>
      </li>

      {{#if user.isAdmin}}
        <li class="nav-item">
          {{#link-to "video" class="nav-link"}}{{entypo-icon "video-camera"}} Videos{{/link-to}}
        </li>
      {{/if}}
      {{#if user.isAdmin}}
        <li class="nav-item">
          {{#link-to "tag" class="nav-link"}}{{entypo-icon "price-tag"}} Tags{{/link-to}}
        </li>
      {{/if}}
      {{#if user.isAdmin}}
        <li class="nav-item">
          {{#link-to "form" class="nav-link"}}{{entypo-icon "text-document"}} Forms{{/link-to}}
        </li>
      {{/if}}
      {{#if user.isAdmin}}
        <li class="nav-item">
          {{#link-to "behavior" class="nav-link"}}{{entypo-icon "hair-cross"}} Behavior{{/link-to}}
        </li>
      {{/if}}
      {{#if user.isAdmin}}
        <li class="nav-item">
          {{#link-to "user" class="nav-link"}}{{entypo-icon "user"}} Users{{/link-to}}
        </li>
      {{/if}}
    </ul>
    <ul class="navbar-nav float-lg-right">
      {{user-nav model=user.model}}
    </ul>
  </div>
</nav>

{{loading-progress class="bg-dark"}}
