query EditFormLineItemQualifications($input: ModelIdInput!) {
  formLineItem(input: $input) {
    id
    form {
      id
    }
    choiceFilters {
      id
      fieldId
      title
      choices
    }
  }
}
