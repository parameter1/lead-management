mutation CampaignEditEmailIdentityFilters($input: EmailCampaignIdentityFiltersInput!) {
  emailCampaignIdentityFilters(input: $input) {
    id
    identityFilters {
      id
      key
      label
      matchType
      terms
    }
  }
}
