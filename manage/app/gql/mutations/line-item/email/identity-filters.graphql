mutation EmailLineItemIdentityFilters($input: EmailLineItemIdentityFiltersInput!) {
  emailLineItemIdentityFilters(input: $input) {
    id
    identityFilters {
      id
      key
      label
      matchType
      terms
    }
  }
}
