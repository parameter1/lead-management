fragment EmailLineItemLinksFragment on EmailLineItem {
  id
  urlGroups {
    url {
      id
      title
      values {
        resolved
      }
    }
    deploymentGroups {
      active
      deployment {
        id
        entity
        name
        typeDescription
        sentDate
        metrics {
          sent
        }
      }
    }
  }
}
